---
- name: Set RTC wake-up
  hosts: all
  become: yes
  tasks:
    - name: Schedule server wake-up for 4 AM
      ansible.builtin.shell: /usr/sbin/rtcwake -m no -t $(date -d "tomorrow 04:00" +%s)
      args:
        executable: /bin/bash

