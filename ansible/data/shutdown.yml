---
- name: Shut down server immediately without password prompt
  hosts: your_ubuntu_server_group
  become: yes
  tasks:
    - name: Shutdown server
      ansible.builtin.command: /sbin/shutdown -h now "Scheduled shutdown"

