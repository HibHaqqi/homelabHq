---
- name: Schedule server shutdown
  hosts: all
  become: yes
  tasks:
    - name: Shut down the server immediately using systemctl
      ansible.builtin.command: systemctl poweroff
      async: 1
      poll: 0

